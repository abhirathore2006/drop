# Google login setup

Drop's login is now generic OIDC — Google is just the default issuer. This page moved to
**[SETUP_SSO.md](SETUP_SSO.md)**, which covers Google (the default), Okta, Microsoft Entra ID,
Keycloak, and Authentik, plus the break-glass admin and a throwaway local Keycloak (`make keycloak`).
